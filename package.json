{
  "name": "event-effect-skeleton",
  "version": "0.1.0",
  "description": "Generalized async-queue architecture skeleton in TypeScript (Event-Reducer-Effect-Interpreter, Outbox, Fencing token, Bounded pool, Hexagonal). Study repo.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "demo": "tsx src/demo/runDemo.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
